#include <iostream>
#include <fstream>
#include <string.h>
#include <stdlib.h>
#include <vector>
#include <string>

using namespace std;

int main()
{
	
	/*Read input file*/
	ifstream fin("abe.jpg", ios::binary | ios::ate);
	long filesize = fin.tellg();
	fin.seekg(0,ios::beg);
	ofstream fout("copy.jpg", ios::binary);

	int bitesize = 1000, count = 0;
	unsigned char *buffer = new unsigned char[bitesize];
	char *copy_string	= new char[filesize];
	
	while (fin.read((char*)buffer, bitesize))
	{
		fout.write((char*)buffer,bitesize);
	}
	fout.close();


	while (1);
	return 0;
}
